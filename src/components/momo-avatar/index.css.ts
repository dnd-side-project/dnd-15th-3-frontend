import { style } from "@vanilla-extract/css";

export const root = style({
  display: "block",
  flexShrink: 0,
});

export const image = style({
  display: "block",
  width: "100%",
  height: "100%",
});
