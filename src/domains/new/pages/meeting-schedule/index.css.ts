import { style } from "@vanilla-extract/css";

export const intro = style({
  marginTop: 35,
});

export const fields = style({
  display: "flex",
  gap: 16,
  marginTop: 24,
  padding: "0 20px",
});
